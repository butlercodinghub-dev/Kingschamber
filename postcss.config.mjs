/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {
      config: "/Users/renobutler/Desktop/KINGS CHAMBER APP/kings-chamber/tailwind.config.ts",
    },
    autoprefixer: {},
  },
};

export default config;
